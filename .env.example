# Server Configuration
NODE_ENV=production
SERVER_PORT=4000

# Database Configuration
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=ecomdb

# Client Configuration 
CLIENT_PORT=80
VITE_API_URL=/api

# Docker Registry Configuration (for production)
DOCKER_REGISTRY=ghcr.io
DOCKER_USERNAME=your-username
TAG=latest
