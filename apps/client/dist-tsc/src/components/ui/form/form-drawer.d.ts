import * as React from 'react';
type FormDrawerProps = {
    isDone: boolean;
    triggerButton: React.ReactElement;
    submitButton: React.ReactElement;
    title: string;
    children: React.ReactNode;
};
export declare const FormDrawer: ({ title, children, isDone, triggerButton, submitButton, }: FormDrawerProps) => import("react/jsx-runtime").JSX.Element;
export {};
//# sourceMappingURL=form-drawer.d.ts.map